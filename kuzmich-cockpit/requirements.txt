# Единый пульт «Кузьмич» — зависимости бэкенда.
# Совместимо с Python 3.8 (Jetson Orin NX, aarch64).
fastapi==0.103.2
uvicorn[standard]==0.23.2
httpx==0.24.1
# Опционально — только для реального управления головой (Arduino Nano по serial).
# На борту обычно уже установлен. На dev-машине не обязателен (мок-режим).
pyserial==3.5
python-multipart==0.0.20

# Camera module
websockets>=10.0
av>=10.0            # PyAV — Python bindings для libavcodec (h264_nvmpi)
numpy>=1.20
